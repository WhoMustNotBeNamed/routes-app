services:
  postgres:
    image: postgis/postgis
    container_name: routes_app_db
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=routes_app_db
